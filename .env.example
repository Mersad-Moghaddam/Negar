# Monorepo coordination env for docker-compose overrides.
# Backend runtime environment lives at backend/dev.env.
# Frontend can use frontend/.env for local overrides if needed.

MYSQL_ROOT_PASSWORD=root
MYSQL_DATABASE=libro
VITE_API_URL=http://localhost:8080/api/v1
